name: CI
on:
  push:
    # branches:
    #   - main
  pull_request:
    # branches:
    #   - main
  workflow_dispatch:

jobs:
 test:
  # runs-on: ubuntu-latest
  runs-on: self-hosted

  steps:
   - name: checkout code
     uses: actions/checkout@v4

   - name: Display message
     run: echo "this is first actions"
 
 shell-test:
  # runs-on: ubuntu-latest
  runs-on: self-hosted

  steps:
   - name: run shell commands
     run: echo "create new action"
   
   - name: Test Success
     run: echo "Test completed successfully"
   

   
  
      
      
